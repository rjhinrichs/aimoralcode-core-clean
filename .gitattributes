# Track large binary formats with Git LFS
*.pptx filter=lfs diff=lfs merge=lfs -text

# Optional: Prevent these from being treated as text
*.png  filter=lfs diff=lfs merge=lfs -text
*.jpg  filter=lfs diff=lfs merge=lfs -text
*.jpeg filter=lfs diff=lfs merge=lfs -text

# Ensure JSON and CSV remain diffable (not managed by LFS)
*.json text
*.csv  text

